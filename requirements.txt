# Zależności produkcyjne.
#
# Lista zawiera wyłącznie zależności bezpośrednie — resztę pip dociąga sam.
# Wcześniej był tu zrzut `pip freeze`, w którym po usunięciu ChromaDB zostały
# jego sieroty: onnxruntime, kubernetes, grpcio, cały opentelemetry i
# chroma-hnswlib. Nikt ich nie importował, a ważyły setki megabajtów przy
# każdym budowaniu i w pamięci instancji.
#
# Narzędzia testowe są w requirements-dev.txt.

# Rdzeń: Django i API
# 5.2 LTS, nie 5.1: linia 5.1 przestala dostawac poprawki bezpieczenstwa
# na 5.1.15, a nowsze podatnosci maja latki dopiero od 5.2.15. Zostanie na
# 5.1 znaczyloby swiadome uzywanie wersji, ktorej nikt juz nie lata.
Django==5.2.17
djangorestframework==3.16.0
djangorestframework_simplejwt==5.5.1
# PyJWT wchodzi jako zaleznosc simplejwt, ale przypinamy go jawnie: to on
# weryfikuje podpisy naszych tokenow dostepu, wiec jego wersja jest decyzja
# bezpieczenstwa, a nie szczegolem cudzej paczki. W 2.9.0 bylo 6 podatnosci.
PyJWT==2.13.0
django-cors-headers==4.7.0
drf-spectacular==0.28.0

# Baza danych i wyszukiwanie wektorowe
psycopg2-binary>=2.9
dj-database-url>=1.0
pgvector==0.4.0

# Zadania w tle
celery==5.5.1
# Redisa nic nie importuje wprost, ale kombu potrzebuje sterownika, żeby
# w ogóle zlecić zadanie. Bez niego .delay() wywala się na
# "AttributeError: 'NoneType' object has no attribute 'Redis'" — niezależnie
# od tego, czy jakikolwiek broker działa.
redis==5.2.1

# Model językowy i dopasowywanie FAQ
openai==1.66.3
RapidFuzz==3.12.2

# Wczytywanie treści: dokumenty i strony WWW klienta
# 6.x, nie 5.4: w 5.4.0 bylo 53 znanych podatnosci, wszystkie tej samej
# klasy - zapetlenie albo wyczerpanie pamieci na spreparowanym pliku.
# Parser chodzi synchronicznie w zadaniu HTTP, wiec taki plik blokuje watek
# gunicorna, a nie zadanie w tle. pypdf 6 dokłada twarde limity i zglasza
# LimitReachedError zamiast mielic w nieskonczonosc.
pypdf==6.16.2
docx2txt==0.9
trafilatura==2.0
# Nic nie importuje tego wprost, ale trafilatura sięga po lxml.html.clean,
# które od lxml 5 jest osobnym pakietem i nie instaluje się samo. Bez tego
# gunicorn w ogóle nie wstaje: ImportError przy imporcie aplikacji WSGI.
# Testy tego nie łapią — pakiet wchodzi ubocznie z zależności deweloperskich.
lxml_html_clean>=0.1
beautifulsoup4>=4.8,<4.13
# 2.34: 2.32.3 przeciekal poswiadczenia z .netrc przy zlosliwie spreparowanym
# adresie (CVE-2024-47081). Sciagamy strony klientow po URL-u z panelu, wiec
# to jedyna z tych podatnosci osiagalna z danych wejsciowych uzytkownika.
requests==2.34.2

# Magazyn wgranych plików (S3, Cloudflare R2, Backblaze B2 — ten sam protokół).
# Bez tego pliki lądują na dysku kontenera, który Render kasuje przy wdrożeniu.
django-storages[s3]==1.14.6

# Płatności
stripe<=8.5.0

# Serwer i konfiguracja
gunicorn==23.0.0
python-dotenv==1.2.3
python-decouple==3.8
python-dateutil==2.9.0.post0
# Wchodzi też jako zależność drf-spectacular, ale importujemy go wprost
# (test spójności render.yaml), więc deklarujemy go jawnie.
PyYAML==6.0.2

# Monitoring błędów
sentry-sdk==2.29.1
